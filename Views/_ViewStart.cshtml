@{
    if (User.Identity !=null && User.Identity.IsAuthenticated)
    {
        if(User.IsInRole("Admin"))
        {
            Layout = "_AdminLayout";
        }
        else
        {
            Layout = "_Layout";
        }
    }
    else 
    {
        Layout = "_Layout"; //public
    }

}
